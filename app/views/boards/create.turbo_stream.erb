<%# turbo_stream.prepend "boards", partial: "boards/board", locals: { board: @board } %>
<%= turbo_stream.prepend "boards" do %>
  <%= render @board %>
<% end %>

<%= turbo_stream.update "new_board" do %>
  <%= render "form", board: Board.new %>
<% end %>