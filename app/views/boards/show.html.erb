<%= turbo_frame_tag dom_id(@board) do %>
   <div class="board bg-green-800 text-white p-4 rounded shadow" style="border: 1px solid #ccc; padding: 10px; margin: 10px;">
      <p><strong><%= @board.id %></strong></p>
      <p><strong><%= @board.name %></strong></p>
      <%= link_to "← Back", boards_path %>
    </div>
<% end %>