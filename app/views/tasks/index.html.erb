<h1>Tasks for <%= @board.name %></h1>

<%= link_to "New Task", new_board_task_path(@board) %>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task.title %> (<%= task.completed ? "✅" : "❌" %>)</li>
  <% end %>
</ul>

<%= link_to "Back to Boards", boards_path %>
<%= notice %>